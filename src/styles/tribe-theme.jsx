/* Tribe Pages Only */
.tribe-shell {
  min-height: 100vh;
  padding: 2rem;
  background:
    radial-gradient(circle at top left, rgba(49,46,129,0.5) 0, transparent 40%),
    radial-gradient(circle at top right, rgba(109,40,217,0.5) 0, transparent 40%),
    linear-gradient(to bottom, #0d0b14, #0b0911);
  color: #ffffff;
}

.tribe-panel {
  background: rgba(255,255,255,0.05);
  border: 1px solid rgba(255,255,255,0.15);
  border-radius: 1.25rem;
  backdrop-filter: blur(12px);
}
